<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <queries>
        <!--如果想要与其他的应用进行AIDL通信的话，需要在这里注册包名的信息-->
        <!--谷歌的文档说明 https://developer.android.google.cn/guide/topics/manifest/queries-element?hl=en-->
        <package android:name="com.xc.testclient" />
    </queries>
    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.KotlinProject">
        <activity
            android:name=".activity.MainActivity"
            android:exported="true"></activity>
        <activity
            android:name=".activity.SkinActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".activity.FunActivity"
            android:exported="true"></activity>
        <activity
            android:name=".activity.OkhttpActvity"
            android:exported="true"></activity>
        <activity
            android:name=".activity.CoilActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".activity.HiltActivity"
            android:exported="true">

        </activity>

        <activity
            android:name=".MainIActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".aidlservice.KtvService"
            android:exported="true">

        </service>
    </application>

</manifest>